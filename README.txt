For mac system, please run the below commands to build and run both frontend and backend.


sudo chmod +x build.sh

./build.sh



For windows system, just run the batch file in your CMD.

***
To login, use the default user in H2 DB:
	username: terry
	password: terrypass

